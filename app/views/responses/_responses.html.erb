<%= paginate responses, remote: true %>
<table class="table table-sm table-bordered table-striped">
  <thead>
    <tr>
      <th>#</th>
      <% survey.eager_questions.each do |question| %>
        <th><%= question.title %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% responses.each do |response| %>
      <tr>
        <td><%= link_to response.id, [response.survey, response] %></td>
        <% survey.eager_questions.each do |question| %>
          <td>
            <% question.choices.each do |choice| %>
              <div>
                <% response.answers_for(question: question, choice: choice).each do |answer| %>
                  <span data-answer-id="<%= answer.id %>">
                    <%= answer.content %>
                  </span>
                <% end %>
              </div>
            <% end %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate responses, remote: true %>
